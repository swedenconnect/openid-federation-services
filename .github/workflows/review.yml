name: Automated review
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  prepare:
    uses: swedenconnect/openid-federation-commons/.github/workflows/reusable-review.yml@workflow
    secrets: inherit
    with:
      require-gh-docker: true